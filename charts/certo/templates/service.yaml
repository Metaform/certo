apiVersion: v1
kind: Service
metadata:
  name: {{ include "certo.fullname" . }}
  labels:
    {{- include "certo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "certo.selectorLabels" . | nindent 4 }}
